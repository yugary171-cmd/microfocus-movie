# Agent 工作约定

本文件适用于整个仓库。工程规范见 `RULES.md`，实现进度见 `docs/status.md`。

## 开始工作

- 先检查 Git 状态并阅读相关代码、测试、`RULES.md` 及对应 `docs/` 文档。
- 区分目标设计与当前实现；未出现在代码、Prisma schema 或 `packages/contracts` 中的能力不得宣称已完成。
- 当前只允许 Mock 内部体验；不得通过关闭闸门、改成示例配置或回退 Mock 来模拟外部可用。

## 修改边界

- 保留用户未提交改动，不覆盖、不恢复、不整理任务范围外文件。
- 不删除项目文件；需要退役时移入 `旧内容/` 并交由人工决定。
- 不执行破坏性 Git 命令，不自动提交、推送、创建 PR、部署或发布。
- 不写入或回显 `.env`、密钥、令牌、版权原件、完整回调和个人信息。
- 新依赖、数据迁移、公开 API、账本语义和安全边界变更必须说明影响并执行相应验证。

## 实现要求

- 前后端交互变更先更新 `packages/contracts`，再实现 API 和客户端。
- 权益、奖励、播放和回调遵守 `docs/architecture.md` 的事务、幂等和不可变事实要求。
- 服务端必须执行认证、角色、所有权和状态检查；客户端状态不构成授权。
- Live provider 未真实实现或验证时保持 fail-closed。

## 验证与交付

- 小改动运行相关 workspace 的 typecheck/测试；跨 workspace、契约、数据库或候选发布变更运行 `npm run check`。
- 检查新增诊断、格式问题和敏感信息；不能把未配置的 lint 当作通过证据。
- 只报告实际运行过的验证，并写明未验证部分。
- 代码、配置或契约变化使状态文档过期时，更新 `docs/status.md`；不要在稳定架构/API 文档中记录完成百分比。
- 未经明确要求不创建 Git commit；交付时列出修改范围、验证结果和剩余风险。
