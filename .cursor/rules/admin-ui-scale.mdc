---
description: 管理端与后续同类界面的控件高度、按钮字号和间距阶梯
alwaysApply: true
---

# 界面尺度

后续页面、组件和其它端的同类表单/按钮按此设计，不要另发明像素。

- 单行 `input`/`select`：高度 `32px`（`--control-height`）。多行 textarea 不套该高度。
- 按钮（含 `.button--small`）：高度 `32px`，字号 `12px`，字重 `500`。
- 间距只允许 `4 / 8 / 16 / 24px`（`--space-1` … `--space-4`）。禁止 6、7、10、12、14、18、20 等中间值。
- 标签在上、控件在下：间距固定 `8px`。用 `.field` 的 `gap`，不要再给 label 加 `margin-bottom`。
- 1px 边框、圆角、图标盒、`--control-height` 不是间距阶梯，不要为凑阶梯去改它们。
- 登录/开通页大面积营销留白可用 `clamp`，但仍优先落在 4/8/16/24 或其倍数，不要再写 6、10、12、18。
- 新样式优先引用 `apps/admin/src/styles/tokens.css` 中的变量，不要硬编码同等像素。
